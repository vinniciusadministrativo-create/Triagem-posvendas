# Variáveis de ambiente do frontend
# Copie para .env e ajuste

# URL da API backend. Deixe VAZIO em produção: o backend serve a SPA na mesma
# origem, então a base relativa (/api) é o correto. Em dev o proxy do Vite cobre.
VITE_API_URL=
